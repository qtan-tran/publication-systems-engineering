\chapter{A System That Holds}
A book becomes easier to maintain when its structure is expressed directly rather than simulated with manual spacing. This synthetic chapter shows the neutral PSE baseline for general prose.\footnote{This synthetic footnote demonstrates the default note treatment.}

\section{Structure before decoration}
A section introduces a second level of hierarchy without changing the reading rhythm. The profile supplies page geometry, running heads, folios, and restrained typography.

\section{A short quotation}
\begin{quote}
A reliable publication system should make ordinary pages feel ordinary, while keeping the production rules explicit.
\end{quote}

The result is intentionally quiet: this profile is the starting point when a title does not need a more specialised semantic or presentational contract.

\PSEIndex{systems}
