\chapter{Change Without Drift}

Shared rules create leverage, but they also create risk. A change intended for one behavior can affect every project that consumes the same core. For that reason, shared changes should be paired with repeatable checks.

\section{Regression as editorial infrastructure}

A smoke test asks a modest question: can a known synthetic project still be built from clean source, and does the resulting PDF satisfy a small set of non-negotiable technical conditions? More sophisticated regression fixtures will be added only after the minimal vertical slice is stable.

\section{A deliberately longer heading for baseline line-breaking behavior}

This section exists to create a mildly stressful heading without turning the core fixture into a typography showcase. Visual regression belongs to a dedicated QA layer; this page is retained as a baseline candidate rather than treating pixel differences as fatal.
